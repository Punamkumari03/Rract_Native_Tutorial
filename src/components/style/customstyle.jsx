import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
     
      backgroundColor: 'red',
        padding: 20,
        borderRadius: 10,
        margin: 10,
        fontSize: 20,
        fontWeight: 'bold',
        color: 'white',
        textAlign: 'center',
    },
  });

  export default styles;